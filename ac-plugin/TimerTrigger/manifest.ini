[ABOUT]
NAME = TimerTrigger
AUTHOR = RacePort Kiosk
VERSION = 1.0
DESCRIPTION = Headless app: writes a marker file the instant the player car starts moving, so the kiosk launcher can start the paid-session timer at the exact moment driving begins instead of guessing with a fixed delay.
